{
  "name": "rfid-rtc-attendance-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack RFID/RTC ESP32 attendance monitoring dashboard with Express, Socket.IO, SQLite, React, Vite, TailwindCSS, and Recharts.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c magenta,cyan \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "start": "npm run start --workspace server",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
